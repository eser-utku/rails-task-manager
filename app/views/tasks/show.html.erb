<h2><%= @task.title %></h2>
<p><%= @task.details %></p>
<p><%= @task.completed %></p>

<%= link_to 'edit', edit_task_path(@task) %>
<%= link_to 'delete', destroy_task_path(@task), method: :delete, data: { confirm: "You sure you want to delete this restaurant?" } %>
